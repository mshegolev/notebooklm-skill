# NotebookLM Skill Dependencies
# These will be installed in the skill's local .venv

# NotebookLM API client (vendored, includes playwright for login)
-e ./vendor/notebooklm-py[browser]

# SOCKS proxy support for httpx
httpx[socks]

# Environment management
python-dotenv==1.0.0
